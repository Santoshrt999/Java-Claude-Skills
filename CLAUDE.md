# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a Java project for building Claude skills, examples, and integrations — AI-powered development tooling using the Anthropic API via the Java SDK.

## Current State

The repository is in early initialization. No source code, build system, or test framework has been added yet. When those are introduced, update this file with:

- Build commands (e.g., `mvn package`, `gradle build`)
- Test commands (e.g., `mvn test`, `gradle test -t <TestClass>`)
- How to run a single test
- Any environment variables required (e.g., `ANTHROPIC_API_KEY`)
